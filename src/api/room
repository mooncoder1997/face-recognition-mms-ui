import request from '../util/request'

export function QueryRoomByRoomName(roomName) {
  return request({
    url: 'http://192.168.1.105:8040/mms-provider-meeting/api/v1/rooms/' + roomName,
    method: 'get'
  })
}
