import request from '../util/request'

export function QueryRoomByRoomName(roomName) {
  return request({
    url: 'http://58.87.120.47:8040/mms-provider-meeting/api/v1/rooms/' + roomName,
    method: 'get'
  })
}
